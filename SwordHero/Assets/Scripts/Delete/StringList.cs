using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StringList : MonoBehaviour
{
    public List<string> killTextList;
    public List<string> list0, list1, list2;
    void Awake()
    {
        //int rdm = Random.Range(0, 4);
        //if (rdm == 0) killTextList = list0;
        //if (rdm == 1) killTextList = list1;
        //if (rdm == 2) killTextList = list2;
    }
}
